{
  "name": "@lanquarden/pi-memini",
  "version": "0.1.3",
  "description": "Pi extension for seamless memini long-term memory over REST (no MCP required).",
  "type": "module",
  "keywords": [
    "pi-package",
    "pi",
    "memini",
    "memory",
    "ai-agent"
  ],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/lanquarden/pi-memini"
  },
  "pi": {
    "extensions": [
      "./extensions/memini.ts"
    ]
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck",
    "test": "node --import tsx --test"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*",
    "tsx": "^4.15.0",
    "typescript": "^5.9.0"
  }
}
